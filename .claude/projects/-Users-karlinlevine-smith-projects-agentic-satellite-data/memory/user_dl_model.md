---
name: User's Deep Learning Crop Classification Model
description: User has trained a transformer-based DL model for crop classification — future integration into the agentic pipeline
type: project
---

User has a custom deep learning model (transformer-based) trained for crop classification. Future plan is to integrate it into the agentic satellite analysis pipeline to show how DL fits into agentic workflows. This is a future phase, not current scope.

**Why:** Shows students the full picture — agentic orchestration + specialized ML models working together.
**How to apply:** When scoping current improvements, keep the architecture open for plugging in a classification model as a tool/skill later. Don't implement now but don't design in a way that blocks it.
